# AI Current

Checkpoint of the active task. Keep focused on current work only; move durable knowledge to AI_MEMORY.md / AI_RULES.md when a task completes.

## Current task
PR #26 implemented (working tree, uncommitted, awaiting Carl's commit): added a `Disabled` property to both slider panels. Design: new `[Parameter] public bool Disabled` on `SliderPanelBase` (after `PanelPosition`); when true the slider div gets a `disabled` class (razor: `@DefaultSliderClass @SliderClassString @(Disabled ? "disabled" : "")`) styled by scoped CSS in both panels (`.disabled { cursor: not-allowed; opacity: 0.5; }`); `ev.preventDefault()` + `if (slider.classList.contains("disabled")) return;` as the first lines of the slider `mousedown` AND `touchstart` handlers in `Sliders.js` (both `registerVerticalSliderPanel` and `registerHorizontalSliderPanel`); and `if (Disabled) return;` guards at the top of `MouseDown`/`MouseUp`/`MouseMove` in both panel `.razor.cs` files. Both test apps' `Index.razor` vertical demo pages got a "Disable slider" toggle button (LeftChildContent) wired to `Disabled="@isDisabled"` with `@code { private bool isDisabled; private void ToggleDisabled() { isDisabled = !isDisabled; } }`. Also in this working tree: the `/visibility` pages in BOTH test apps were missing the `<NavMenu />` that every other demo page has (Carl reported being stranded on the page) — added it to the blue right panel of each, just below the first paragraph; browser-verified all 11 links render there and navigate (server app) / render (wasm app). Follow-up: the menu text was invisible (black on the #4444ff panel) because NavMenu.razor.css colors its links via `.nav-item ::deep .nav-link` but NavMenu.razor never wraps links in `.nav-item` — so its link colors never apply anywhere and links fall back to default black. Fixed page-locally: the blue panel div got `class="blue-panel"` and a new `Visibility.razor.css` (both apps) uses `.blue-panel ::deep .nav-link` (+ :hover/.active) to force white text; other pages' menus are untouched (verified: white on /visibility, default dark on /). Also added `margin-bottom: 1rem` on `.nav-link:last-child` in that scoped CSS so "Right Line 1" doesn't crowd the last menu item (verified 16px gap in browser).

The stale Copilot draft PR #26 was NOT the source of this change — it was re-implemented cleanly. While verifying, a pre-existing robustness gap surfaced and was fixed as part of this PR: Chrome starts a native text-selection (first press: `selectstart`) and then native HTML5 drag (subsequent presses: `dragstart`) over the slider — since PR #28 the slider has child content (the SliderContent host div covers the whole bar) — and the native drag converts `mousemove`→`drag` and `mouseup`→`dragend`, so the app's mouse handlers never fire and the slider only moves one mousemove step. Fixed by `ev.preventDefault()` as the first line of the slider mousedown/touchstart handlers (standard for custom drag widgets). Verified: `dotnet build --no-incremental` → 0 errors / 10 baseline warnings; headless Playwright acceptance test (`pr26-test.js` in session files) passes all steps — enabled drag, disable (class/opacity/cursor), disabled drag fully blocked (delta 0), re-enable drag works; 3-consecutive-drag probe clean of `dragstart`/`selectstart`; horizontal page (`pr26-horiz.js`) drags incl. repeat press pass.

## Next steps
Deliver one-paragraph commit message; Carl commits. Stale draft PRs #26, #28, #30 are all CLOSED (2026-08-26, with explanatory comments) — no open PRs remain. Issues #3, #27, #29, #33 were also CLOSED (2026-08-26, with comments). Remaining open issues: #31 (parentContained resize-listener null check — the one actionable bug, if he wants it fixed) and #4 (2021 feature request — Carl's call).

## Docs workflow transient failure (2026-08-26, post-push 64b4f65)
- on-push-do-docs run 32985905193 FAILED in 8s with NO logs (job never started; `gh run view --log` → 'log not found'; identical run 12 min earlier succeeded in 27s) → transient GitHub Actions infra hiccup, not a content failure.`gh run rerun` refused ('cannot be retried').
- Fix: added `workflow_dispatch:` to on-push-do-docs.yml so it can be triggered manually (Actions tab → Run workflow); Carl's commit+push of this change will also trigger a fresh push run and confirm the pipeline is healthy.

